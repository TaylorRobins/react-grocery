Is there a better way to implement your search?

A better way to implement a search would be to involve the database.

What is the difference between in-memory storage and persisted storage? Where can persisted storage exist? 

In memory storage lives in RAM, and persisted storages lives in persistent media like a hdd or ssd.


What other data could you leverage for route parameters other than an ID? 

You could use a media url.
